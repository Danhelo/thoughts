From RepE paper (https://arxiv.org/pdf/2310.01405) and work on metanets:

Create an AI that learns its own representation space. Then RLHF it on human emotionality so its able to finetune its own neuronal activations, adding or removing emotions or other features at inference time.

This might be how we are wired, with a constant stream of features being activated by the metanet, then using those activated features to predict next likely activation features. 